<UserControl x:Class="DotNetProject.Views.PlayerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="395.276" d:DesignWidth="611.771"
             >
    <Grid Margin="0,0,-147,-24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="55*"/>
            <ColumnDefinition Width="14*"/>
        </Grid.ColumnDefinitions>
        <Button Content="Play" Command="{Binding PlayCommand}" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="75"/>
        <Button Content="Pause" Command="{Binding PauseCommand}" HorizontalAlignment="Left" Margin="103,10,0,0" VerticalAlignment="Top" Width="75"/>
        <Button Content="Stop" Command="{Binding StopCommand}" HorizontalAlignment="Left" Margin="194,10,0,0" VerticalAlignment="Top" Width="75"/>
        <Button Content="Browse" Command="{Binding BrowseCommand}" HorizontalAlignment="Left" Margin="284,10,0,0" VerticalAlignment="Top" Width="75"/>
        <MediaElement Name="mediaElement" Source="{Binding Filename}" LoadedBehavior="Manual" HorizontalAlignment="Left" Height="335" Margin="10,47,0,0" VerticalAlignment="Top" Width="582"/>
        <Label Content="{Binding Duration}" HorizontalAlignment="Left" Margin="416,10,0,0" VerticalAlignment="Top"></Label>

    </Grid>
</UserControl>
