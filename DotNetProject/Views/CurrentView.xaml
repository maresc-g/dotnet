<UserControl x:Class="DotNetProject.Views.CurrentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             d:DesignHeight="400"
             d:DesignWidth="500"
             mc:Ignorable="d">
    <Grid AllowDrop="True" DragEnter="Grid_DragEnter" Drop="Grid_Drop">
        <ContentControl Name="Music" Content="{Binding MusicViewModel}" Margin="0,0,0,0" Visibility="Hidden"/>
        <ContentControl Name="Video" Content="{Binding VideoViewModel}" Margin="0,0,0,0" Visibility="Hidden"/>
        <ContentControl Name="Image" Content="{Binding ImageViewModel}" Margin="0,0,0,0" Visibility="Hidden"/>
        <ContentControl Name="Player" Content="{Binding PlayerViewModel}" Margin="0,0,0,0" />
    </Grid>
</UserControl>
